#!/usr/bin/env python3
import os
import sys

# Add the parent directory to Python path so we can find the 'howdoi' package
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from howdoi.cli import main

if __name__ == "__main__":
    sys.exit(main())
